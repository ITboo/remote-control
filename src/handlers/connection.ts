//duplex const duplex = createWebSocketStream(ws, { encoding: 'utf8' });
import {
    drawSquare,
    drawRectangle,
    drawCircle
} from '../commands/commands'

/*switch (command) {
    case ('up'): {
        await
      break;
    };
    case ('down'): {
        await
      break;
    };
    case ('left'): {
        await
      break;
    };
    case ('right'): {
        await
      break;
    };
    case ('position'): {
        break;
    };
    case ('draw_square'): {
        drawSquare();
        break;
    };
    case ('draw_rectangle'): {
        drawRectangle();
        break;
    };
    case ('draw_circle'): {
        drawCircle();
        break;
    };
    case ('screenshot'): {
        break;
    };
    default: {

    }
}*/